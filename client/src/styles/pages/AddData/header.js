import styled from "styled-components";

const Header = styled.header`
  display: block;
  width: 80vmin;
  margin: 0 auto;
  text-align: center;
  h1 {
    font-size: 4rem;
    color: dimgray;
  }
`;

export default Header;
